apiVersion: v1
kind: Service
metadata:
    name: php7.4-base
    labels:
      app: php7.4-base
spec:
    selector:
        app: php7.4-base
    type: ClusterIP
    ports:
        - protocol: TCP
          port: 9000
          targetPort: 9000
